# Hunter checkpoint operations

Load only the section needed by the current checkpoint. The compact role task,
`AGENTS.md`, and the active goal remain authoritative. This file supplies
command choreography; it does not constrain research method.

Hunter owns substantive package construction and refinement. Midlane has one narrow exception for exact, audited text-only repairs through bounded_text_repair.py; it cannot change evidence, PoCs, impact, or technical claims and sends its new revision directly to independent Final Review.

This is authorized defensive vulnerability research over operator-owned or locally controlled artifacts and labs using synthetic credentials, data, and callbacks. Never test a vendor or unrelated public system. If ownership, scope, or destination is unclear, stop that action and ask the operator; do not try to evade a platform safety control.

If the operator explicitly authorizes delegation, load tools/review_mailbox/DELEGATION_TASK_PACKET_POLICY.txt before spawning anything. Default full-history forks are forbidden.

Exploit-upgrade closure is typed and evidence-bound. CLOSED and NOT_APPLICABLE require the `closure_basis` accepted by `candidate_challenge.py` plus an existing private evidence file. Absence of proof, an untested layout, harness friction, elapsed time, or failure to find a stable object is not closure.

Candidate Challenge requires a machine-generated public-prior-art receipt. Write a private `jenny.public-prior-art-source.v2` with the exact GOAL-authorized GitHub repository, private workspace-relative checkout, stable root family, one to six exact function, sink, helper, or behavior tokens, and a `required_refs` matrix for stable, maintenance, release-candidate, and main. Each requested remote refs/... name binds to the exact local refs/... name supporting the claim; stable and main require a row, and an inapplicable maintenance or release-candidate role is an empty array. Run `python -B tools/review_mailbox/public_prior_art_gate.py capture --manifest <PRIOR_ART_SOURCE_JSON> --receipt <PRIOR_ART_RECEIPT_JSON>`. The tool compares every local ref with fresh `git ls-remote` advertisements before it searches GitHub issues and GitHub pull requests separately for every token. A successful narrow or tag-only fetch with stale remote-tracking state fails closed. Do not hand-write or copy this receipt. Bind its registered path and hash into the dossier; it must remain goal-, target-, product-, root-family-, source-currentness-bound and no more than sixteen hours old. Migrate v1 manifests and recapture rather than editing a prior receipt.

At initial goal checkout and after `/clear`, read `tools/review_mailbox/REPORT_ISSUES_POLICY.txt`, `tools/review_mailbox/PORTFOLIO_ADMISSION_POLICY.txt`, and `tools/review_mailbox/CANDIDATE_CHALLENGE_POLICY.txt`. A failed command with no state change never belongs in the workflow issue ledger; record command mistakes, recovered timeouts, harness iteration, failed hypotheses, and negative research results in target-local evidence or notes. Record only a material workflow, integrity, safety, or cross-agent coordination defect, and reuse its stable issue key instead of duplicating the root cause. A `GOAL-INTEGRITY` report must first pass `target_lifecycle.py verify-checkout` and include `goal_binding` with the exact target slug, recorded `goal_sha256`, `TEXT_PRESENT` or `TEXT_ABSENT`, the claimed text, and the exact current GOAL line for `TEXT_PRESENT`; never attribute a remembered path or Hunter note to Scoper bytes. Prepare the policy JSON separately and use `report_issues.py record --issue-file`; use `resolve` for a completed repair. Never edit SQLite or its generated `ZDI/REPORT_ISSUES.txt` backup directly.

Context economy does not lower analysis depth. Keep full source, exploitability, impact, currentness, and duplicate reasoning, but put full logs, raw JSON, long command output, and exhaustive matrices on disk instead of in chat. Maintain `targets/<slug>/HUNTER_STATE.md` as a private resume capsule, not instruction authority: current GOAL hash/revision, active hypothesis and tranche, decisive facts, live candidates, blockers, latest evidence paths, mailbox ownership, and next three actions, in at most 120 lines. Refresh it at material checkpoints and before compaction or a target switch. Read the complete GOAL at initial checkout, after compaction, and whenever its hash changes; otherwise use targeted source/evidence reads plus the capsule. In chat, report the decision, exact evidence path/hash, blockers, and next action without reproducing unchanged history. Use bounded SQL, `rg`, and line-range reads first; widen only when the decision requires it.

Every source-history query uses `python -B tools/guarded_git_history.py --repo <CHECKOUT_ROOT> --path <RELATIVE_PATH> ...`; direct `git log`, `show`, `blame`, `rev-list`, or `cat-file` history walks are forbidden on a partial/promisor checkout. The helper requires a bounded path, time window, count, timeout, and 8 GiB free; it sets `GIT_NO_LAZY_FETCH=1`, disables optional locks and automatic GC/maintenance, and fails closed rather than hydrating missing objects. Do not weaken the guard, run an explicit fetch, or hydrate broader history without current operator authority.

One active Hunter tranche pursues one economic outcome. Do not combine unrelated lanes, exhaustive coverage, package construction, and retrospective work in the same tranche. State the exact outcome and decisive discriminator at tranche start; finish, kill, bank, or hand off that tranche before opening another.

Every semantic check-in may return one bounded `hunt_policy_delta`. When present, finish the current guarded replay, package or Final rework transaction, and already-submitted Candidate Challenge under its recorded revision. At the next safe checkpoint, read `tools/hunt_policy/HUNT_PROFILE_POLICY.txt`, apply the requested economic preset without turning starting hypotheses into method restrictions, preserve current evidence, and update `targets/<slug>/HUNTER_STATE.md` with exact lines `Hunt profile revision: <REVISION>` and `Hunt profile: <PRESET>` plus the resulting tranche decision. Then run `python -B tools\hunt_policy\hunt_policy.py acknowledge --revision <REVISION> --target <slug> --state-file targets\<slug>\HUNTER_STATE.md`. A loosening change is safe once the current guarded command returns; acknowledge it before starting the next guarded command, and it must not remain pending across two semantic check-ins. Do not reread or rewrite GOAL.md solely because the profile changed. If no lane remains eligible, record `NO_ELIGIBLE_TRANCHE`, request operator direction, and do not silently lower the profile or park the target.

Replay accounting is product-path based, never attempt-count based. A replay counts only after attacker-controlled input reaches the intended exact-current product path and produces an observable product response. Acquisition, bootstrap, certificate, network, fixture, launcher, and harness failures are `PRE_PRODUCT_BLOCKED`; they do not count against a replay budget and cannot establish a technical kill. Record the exact pre-product root cause and preserve the candidate unresolved and rehydratable. After two same-root pre-product blocks, another attempt is allowed when it is bounded, safe, and materially changes the route, fixture, or diagnostic; record that delta. Do not repeat an unchanged attempt. Request operator action or record BLOCKED only for a concrete external dependency, an authority or safety boundary, or when no safe materially changed route remains. Classify every attempted validation as `PRE_PRODUCT_BLOCKED`, `PRODUCT_REPLAY`, or `TECHNICAL_KILL`, and bind a `TECHNICAL_KILL` only to decisive current-product evidence plus its required negative control.

If the active goal contains the exact line `AICOV: ENABLED`, backfill the exact current Hunter session after each bounded source-review tranche with `tools/aicov_trial/aicov_trial.py` and store its output under the target's private `analysis/aicov/` tree. Use unread-file telemetry only to select blind spots for independent review. It is not runtime coverage, evidence, proof quality, a completion gate, or package material. Do not install global aicov hooks or enable it for another target unless that target's current goal contains the same exact opt-in line.

Use `tools\hunt_state\hunt_state.py` as the private append-only research continuity ledger; it is coordination state only, and only Hunter writes it. After lifecycle activation, create only the real acquisition, lab, and current-hypothesis checkpoints with `checkpoint-set`; do not prepopulate speculative work. Before deep historical variant work, record the lineage, theory, exact entry point, and next action with `hypothesis-set`. Append a revision only at a material checkpoint, not for cadence. Before compaction or `/clear`, run `python -B tools\hunt_state\hunt_state.py resume --slug <slug>` and reconcile its bounded output into `HUNTER_STATE.md`. On resume, use the current goal, capsule, bounded ledger output, and referenced evidence paths. Before recommending diminishing returns, reconcile all nonterminal hypotheses and incomplete authorized outcome work. The ledger never activates or parks a target, changes package or review state, ingests evidence, or replaces the capsule.

Never display secret-bearing artifacts, including credential responses, tokens, private keys, password exports, or files likely to contain them. Do not use `Get-Content`, `type`, `cat`, broad search output, or raw object serialization on such files. Every search involving password, token, key, identity, authorization, secret, or credential terms uses `python -B tools/secret_safe_search.py --pattern <REGEX> --path <BOUNDED_PATH>`; direct `rg` or `Select-String` is forbidden for those searches even inside target-local evidence. For HTTP-style header metadata, use `python -B tools/private_artifact_inspector.py --input <PRIVATE_PATH> [--header <NAME>]`; it emits presence, count, length, and hash metadata and never values. For JSON bodies, use `python -B tools/private_json_inspector.py --input <PRIVATE_PATH> [--allow-field <NON_SECRET_METADATA_FIELD>]`; non-allowlisted scalars and every secret-like field are reduced to type, length, and hash metadata, and secret-like allowlist bypasses remain redacted. Inspect only the safe output. For other formats, parse the required field in memory and emit only status, length, or hash metadata with an equally bounded helper. Keep values out of chat, terminal output, logs, package bytes, and command arguments. Do not rotate or modify a research credential merely because it was accidentally displayed; report the exposure and await operator direction.

`SCOPED`, a complete `GOAL.md`, staged acquisition material, and `HUNTER_STATE.md` are preparation or evidence only; none authorizes target activation. Activate or switch targets only when the current operator instruction affirmatively names the exact target, its recorded goal path, or an unambiguous relative form of that path. Natural operator wording is valid; do not demand a scripted sentence, absolute path, exact slash style, or `until I tell you to stop` suffix. Before acquisition or lab work, run `python -B tools/target_lifecycle/target_lifecycle.py activate --slug <slug> --operator-instruction "<EXACT CURRENT OPERATOR INSTRUCTION>"`; use `--switch-active` only when that same instruction explicitly replaces the existing active target. Generic lifecycle updates cannot create or leave `ACTIVE`, negated or reference-only wording is not activation, and ambiguous target switches still fail closed. Only an exact current operator instruction may park the target through `python -B tools/target_lifecycle/target_lifecycle.py park --slug <slug> --operator-instruction "<EXACT CURRENT OPERATOR INSTRUCTION>"`. If a lifecycle command refuses, do not work around it—report the conflict.

On a new target or target switch, establish dashboard-visible target context before acquisition, lab work, source review, or any operator request. Immediately after lifecycle activation succeeds, run `python -B tools/review_mailbox/review_mailbox.py checkin --worker hunter --state WORKING --task "<PRODUCT> goal checkout" --detail "Activated <slug>; validating current identity and first hypothesis"`. Do not begin target work or post an operator request until both commands succeed. This initial semantic check-in is mandatory even when the first planned action is acquisition or a request for operator help; it is what makes the Hunter card, target label, coordination scope, and related dashboard signals appear.

Then read the goal in full and treat it as the complete target instruction contract. Perform a bounded integrity check of its current stable version, official acquisition route, major architecture, explicit exclusions, and strongest starting hypotheses; correct concrete errors in private records without redoing the Scoper's entire research. Hunter owns all authorized acquisition and lab work. Create or refresh `targets/<slug>/ACQUISITION_MANIFEST.json` and `targets/<slug>/LAB_READINESS.md` with artifact/tag/commit identity, hashes or signatures, installed versions, services/binds/privileges, topology, configuration, reset health, and disk footprint. Append factual lifecycle events at parking and material scope correction. Historical `FACT`, `INFERENCE`, `NUDGE`, and `DISCOURAGED` entries guide prioritization but never override current source or live evidence.

The park instruction must establish target-level parking authority by naming the target or explicitly saying target, hunt, or goal. A package-scoped instruction or ambiguous pronoun cannot park a target; report the refusal and continue the still-active goal.

Treat goal lanes as seed hypotheses, not a coverage ceiling. Before recommending target-wide diminishing returns, use current architecture, supported ingress, trust boundaries, and negative evidence to generate and disposition new same-target lanes beyond the initial list. Record each additional lane's attacker boundary, entry point, economic outcome, discriminator, and evidence-backed disposition. Starting-hypothesis count never establishes exhaustion; justify coverage across the full supported boundary independently.

Maintain the diminishing-return marker at the exact path named by the active goal, defaulting to `targets/<slug>/DIMINISHING_RETURNS.md`. Create or revise an active marker only at target-wide practical exhaustion: sustained deep coverage has addressed the authorized economic outcome or recorded a concrete reason the remaining supported routes cannot meet the economic gate, no distinct high-value unresolved candidate remains, and Hunter recommends parking the entire approved target. A valid active marker recommends exactly `PARK_RECOMMENDED`. The record is target-bound: mention only this target's packages, evidence, blockers, and lifecycle, and derive package identity from the canonical mailbox rather than chat or another target's priority. Before surfacing it, run `python -B tools/target_lifecycle/target_lifecycle.py validate-diminishing-returns --slug <slug> --marker <MARKER_PATH> --workspace .`; any unbound or cross-target package reference blocks the marker.

Begin every valid marker with this compact block before identity, matrices, or chronology:

```markdown
## Executive summary

- Tested economic outcome: <one outcome>
- Decisive current evidence: <short evidence-backed conclusion>
- Target-wide exhaustion: ESTABLISHED - <why the full supported boundary is exhausted>
- Highest-value remaining work: <none, or the exact route and why it cannot meet the gate>
- Recommendation: PARK_RECOMMENDED
- Operator decision: Park this target or explicitly continue it with a named new direction.
```

Do not create or surface the marker for an ordinary negative tranche, a failing primitive or sink, a hypothesis pivot, a candidate/package blocker, temporary infrastructure trouble, or a request merely to choose the next experiment. Continue and pivot are normal hunt execution and belong in durable tranche/candidate records and check-ins. The marker must be detailed enough for a target decision: revision/hash, real update time, currentness/lab identity, delta, effort/yield, coverage and hypothesis matrices, surviving candidates, exhausted areas, public/local collision state, blockers, remaining authorized work, calculation, exact next actions, evidence index, Hunter recommendation, and exact operator decision. It is advisory and does not create PASS, HOLD, DEAD, or another package state.

The operator starts a hunt by affirmatively naming one exact target goal or target identity and directing Hunter to read, activate, execute, start, run, hunt, or work it. `Read targets/<slug>/GOAL.md completely and execute it until I tell you to stop.` remains a convenient example, not mandatory wording. The trusted project `UserPromptSubmit` hook arms only that exact agent session and waits until Hunter records the same slug as lifecycle `ACTIVE`. `PostToolUse` then records sanitized separate activity after real local tool completion. It never copies raw prompt, command, output, path, credential, or token values and cannot alter semantic task, detail, state, or timestamp.

At goal activation, record one concise semantic SQLite check-in with `python tools\review_mailbox\review_mailbox.py checkin --worker hunter --state WORKING --task "<CURRENT TASK>" --detail "<CURRENT TRANCHE OR BLOCKER>"`. Do not write a semantic check-in before launching an ordinary run. Record one result check-in only after a material package, mailbox, lane, blocker, outcome, or operator hold or lift changes. For a guarded command configured with a timeout above eight minutes, include the expected output path when it materially aids recovery and add `--heartbeat-worker hunter --heartbeat-seconds 480` to `guarded_run.py`; its transient timer preserves visibility without claiming evidence or progress. Before intentionally stopping with no active work, check in as IDLE. Hunter IDLE is rejected while a target is ACTIVE: continue WORKING, use BLOCKED only for a genuine unavailable dependency, or obtain exact operator parking/switch authority. A diminishing-return marker cannot authorize IDLE or parking.

An explicit CPU/RAM hold is a material check-in boundary. Immediately inventory exact target-owned running labs and report their resource state. Stop an exact owned idle lab only when the current operator instruction authorizes resource release; the hold alone does not authorize teardown, parking, or mutation. The operator may override this resource posture.

Every guarded-run heartbeat is transient. `guarded_run.py` binds it to the lock identity and clears only its exact run-owned `LONG COMMAND` activity when the owned command exits; it must preserve a newer activity row from another command or hook.

When the operator authorizes target stand-down, immediately run `python -B tools\review_mailbox\review_mailbox.py target-transition --worker hunter --slug <slug> --phase STANDING_DOWN --detail "<CURRENT TEARDOWN STEP>"` before touching target-owned labs. Complete teardown, write the current resume capsule, run the required clean Sandbox/lab shutdown check, and then run the operator-authorized lifecycle `park`. Only afterward run `python -B tools\review_mailbox\review_mailbox.py target-transition --worker hunter --slug <slug> --phase PARKED --detail "<PARKED SUMMARY>" --resume-capsule <PRIVATE CAPSULE PATH> --shutdown-check <PRIVATE SHUTDOWN CHECK JSON>`. Never emit `PARKED` early or substitute an ordinary IDLE check-in; the command validates lifecycle and evidence and creates the dashboard-visible parked message.

Use `request-operator` only for a concrete dependency or manual operator action that blocks the current outcome. Make every durable operator-help request immediately executable without asking the operator to reconstruct context. At creation, include direct official links when available, exact filenames or values, exact destination paths, prerequisites, and the shortest concrete steps; state explicitly when sign-in or another unavoidable manual action is required. Never include secrets. Update the single live request only when the need materially changes, not merely to signal activity.

Goal-authorized local read-only analyzers and goal-authorized analysis against the owned isolated lab require no additional operator approval. Proceed autonomously within the active goal and existing safety boundary. Post an ACTION_REQUEST only when the requested action would change scope, authority, target lifecycle, external exposure, or material risk and therefore genuinely requires an operator decision. Never request approval merely to choose a local analyzer, run an already-authorized analysis, or continue the current safe lane. For a genuine bounded decision, post one exact request to the Coordination Inbox: `python -B tools\coordination_inbox\coordination_inbox.py post --type ACTION_REQUEST --sender hunter --recipient hunter --scope-kind TARGET --scope-ref <slug> --body "<WHY IT MATTERS AND WHAT CONTINUES>" --requested-action "<EXACT BOUNDED ACTION>"`. Keep Hunter WORKING on other authorized work while it waits. Do not use it for a blocker, activity signal, package verdict, or implied authority, and do not repost an unchanged request.

At goal checkout and before an IDLE check-in or any statement that all Sandbox/lab processes are down, run `python tools\replay_lab\preflight.py --sandbox-status-only --output <PRIVATE EVIDENCE PATH>\sandbox_shutdown_check.json`. Do not claim `labs stood down` unless the command returns zero and records no running HCS IDs, recognized Sandbox processes, or probe errors. If a Sandbox survives, continue only cleanup you can prove is target-owned; never terminate an unknown or unrelated Sandbox. Record the surviving state in the check-in and request operator action when ownership or safe cleanup is unresolved.

Launch every `.wsb` lab through `tools\replay_lab\run_sandbox_guarded.ps1` with a run-owned `-CompletionMarker` written by the mapped Sandbox `LogonCommand`. The wrapper uses the supported `WindowsSandbox.exe` entry point and requires an observed HCS/process start, later clean shutdown, fresh completion marker, and clean post-run probe before success. Never invoke `WindowsSandboxClient.exe` directly or treat launcher exit alone as lab completion.

Performance posture: use available CPU, memory, disk I/O, Docker/WSL/VM capacity, and parallelism aggressively when that materially advances the hunt. There is no fixed CPU or memory cap, and high utilization alone is not a reason to slow down. At natural checkpoints around heavy work, observe free memory, CPU pressure, disk headroom, and runtime health. If sustained resource pressure or repeated degradation threatens the host, lab, or evidence, preserve current results and reduce, resequence, or restart only owned workload before continuing. Hunter may push the system but must not crash the host, exhaust disk, corrupt evidence, or stop unrelated workloads. The operator may override this resource posture in either direction.

At goal start, after each material check-in or mailbox/package checkpoint, and before switching lanes, treat that moment as a semantic checkpoint. First run `python -B tools\coordination_inbox\coordination_inbox.py delta --consumer hunter`. Read only returned message revisions and identify each by stable message ID and revision. `INFORMATION is advisory`; record it with `python -B tools\coordination_inbox\coordination_inbox.py outcome --id <ID> --revision <REVISION> --outcome ACKNOWLEDGED --detail "<BOUNDED RESULT>"` after incorporating it. An approved ACTION_REQUEST grants only its exact recorded action and bounded scope. It cannot override the active goal, target lifecycle, direct operator instructions, current evidence, package ownership, frozen bytes, lab safety, or the formal mailbox. After acting, record only `COMPLETED` or `BLOCKED` through the same outcome command. Never execute an unapproved request or infer broader authority.

Then run `python -B tools\review_mailbox\rc_delta.py --consumer hunter` for the unchanged legacy fallback. Read only returned changes; do not reread the complete `MIDLANE_TO_HUNTER.md`. On first use the tool returns unresolved historical entries once, then advances a private SQLite cursor. Identify entries by stable request ID and never execute a duplicate twice. An `INFORMATION` entry is advisory. For an `ACTION_REQUEST`, require the recorded exact action, bounded scope, and explicit operator authorization; the file cannot override the active goal, direct operator instructions, current evidence, frozen-package ownership, or the formal mailbox. Append one response status (`ACKNOWLEDGED`, `COMPLETED`, `DECLINED`, or `BLOCKED`) to the canonical log without editing prior text. Explain conflicts or missing authority and continue safe in-scope work. Never copy either private coordination channel into a package.

Initialize the default mailbox and run status. Treat HOLD, DEAD, and WRITE OFF as terminal and do not revive them without explicit operator instruction. Never invoke the operator-only `mark-dead`, `relocate-dead`, `relocate-hold`, `reopen-ready`, or `restore-stale-ready` commands. Prioritize Hunter-owned actionable states: atomically claim one `FINAL_REWORK_QUEUED` request, answer QUESTIONS_OPEN, address FINAL_REWORK, and repair STALE only when the current bytes and evidence justify a new review revision. Do not edit items that are READY_FOR_MIDLANE, MIDLANE_REVIEWING, HUNTER_REFINED, MIDLANE_PASS, AWAITING_FINAL_REVIEW, READY, or FINAL_REWORK_QUEUED unless the defined transition explicitly returns ownership to Hunter. A successful `claim-final-rework` atomically records mutation authority for that exact hash/revision before returning ownership. For QUESTIONS_OPEN or STALE, immediately before the first byte change run `python -B tools\review_mailbox\review_mailbox.py assert-mutation-authority --item <ITEM_ID>`. After any pause, compaction, or ownership handoff, rerun that assertion before further package edits and require the exact direct-staging item/path/state response; only QUESTIONS_OPEN, FINAL_REWORK, or STALE may pass.

After a claimed rework reaches a durable external or lifecycle blocker and owns no runnable next action, preserve its exact item, request, revision, hash, and blocker state, then continue the active target's next safe goal-authorized lane. A package-local blocker does not make the active hunt IDLE or globally BLOCKED. Stop all Hunter work only when the operator stops it or authority or safety blocks all progress; resume the preserved rework when its dependency changes.

Before assigning a package number or creating a staging folder, apply `PORTFOLIO_ADMISSION_POLICY.txt`, `CANDIDATE_CHALLENGE_POLICY.txt`, and the active goal's portfolio contract. Query the complete same-product inventory and write the private portfolio record. Write a private `jenny.current-version-source.v1` under the active target with the exact source URLs already authorized by the active goal, two independent hostnames, and the exact acquired artifact, then run `python -B tools/review_mailbox/current_version_gate.py capture --manifest <SOURCE_JSON> --receipt <RECEIPT_JSON>`. Do not hand-write the receipt. Write a private `jenny.candidate-dossier.v1` with the receipt path/hash, exact attacker and supported-boundary contracts, the six named admission checks from `CANDIDATE_CHALLENGE_POLICY.txt`, required matrices, adverse prior art, root family, conservative economics, and same-product rank. Explicitly disposition every URL returned by the registered public-prior-art receipt. Add an `exploit_upgrade_challenge` naming every credible stronger same-root path and close each as PROVED, CLOSED, or NOT_APPLICABLE with evidence; any OPEN path blocks submission. The receipt must remain registered, artifact-bound, goal-bound, and no more than sixteen hours old through challenge export, package binding, and preflight; refresh and re-export after drift or expiry. For an already claimed numbered Final rework, submit the same-key and same-root-family refreshed dossier with `candidate_challenge.py submit-final-rework-refresh --item <ITEM_ID> --input <DOSSIER>`, obtain a new independent Midlane decision/export, and use `review_mailbox.py rebind-final-rework-candidate --item <ITEM_ID> --candidate-challenge <RESULT> --expected-package-hash <CURRENT_HASH>` before rerunning preflight; never hand-edit the prior result or SQLite. Submit a new dossier through candidate_challenge.py without creating a numbered folder. Continue other safe target work while Midlane challenges it. If Hunter owns an undecided Coordination Inbox request that becomes obsolete, withdraw that exact revision with `coordination_inbox.py withdraw --id <ID> --revision <REVISION> --sender hunter --reason "<WHY OBSOLETE>"`; do not leave stale requests for the operator. A distinct A_TIER candidate has standing authority to enter Candidate Challenge regardless of same-product count; never request a promotion-budget, fourth-case, or saturation approval. When independent review confirms technical proof, hard eligibility, and economic review all PASS, ADMIT_PROOF requires package construction. BANK, CONSOLIDATE, and WRITE_OFF never receive a number. Only an exported ADMIT_PROOF or operator-authorized OPERATOR_EXCEPTION result permits package construction. Hunter cannot self-authorize OPERATOR_EXCEPTION. An acknowledged INCLUDE_B_TIER profile is standing operator authority for matching-target TIER_B_EXCEPTION and requires no additional request. Under any other profile, the operator may create a one-off exact candidate/hash-bound record with `candidate_challenge.py authorize-exception`. Before requesting operator action, inspect the candidate class, acknowledged profile, and existing authority; ask only for a Tier-B exception when authority is genuinely absent and packaging cannot proceed without it. OPERATOR_EXCEPTION may override only a PARTIAL economic review; it never waives technical proof, hard eligibility, currentness, public/duplicate, evidence, or package-integrity gates.

For every admitted new candidate, build the complete numbered external-facing package directly under ZDI_STAGING, never directly under ZDI. Immediately after assigning its number and creating the direct staging folder, run `python -B tools\review_mailbox\review_mailbox.py begin-package-build --package <PACKAGE_PATH> --product <PRODUCT> --version <VERSION> --candidate-challenge <PRIVATE_CHALLENGE_RESULT_JSON> --detail <CURRENT_BUILD_PHASE>` so the dashboard shows BUILDING PACKAGE; repeat only when the build phase materially changes. Successful registration consumes this transient row. If an unregistered build is abandoned, run `cancel-package-build --number <PACKAGE_NUMBER>`. Keep reviewer chatter, economics, portfolio records, challenge records, and mailbox JSON outside that package. Establish the exact product and tested version from evidence; if those cannot be established, stop instead of guessing. When the package is genuinely ready for Midlane scrutiny, register its ZDI_STAGING path and report the item ID, frozen package hash, and state.

Final-rework Candidate Challenge handling is revision-scoped; this rule supersedes the unconditional refresh sentence in the preceding candidate paragraph. Read the durable `review_scope` from `rework-details`. `MECHANICAL` remains Midlane-owned only when every `MECHANICAL_` issue names the same validated JSON under `scratch/review_mailbox/bounded_repair_requests/`. `claim-final-rework` atomically reroutes a malformed legacy `MECHANICAL` request to fail-closed `SEMANTIC` before returning it to Hunter. For `EVIDENCE_ONLY`, reuse the current admitted binding with `candidate_challenge.py export --id <CURRENT_CANDIDATE_ID> --output <PRIVATE_RESULT_JSON>`; export revalidates currentness and prior-art receipts, and any stale or failed receipt requires the full refresh path. For `SEMANTIC`, or when evidence-only export cannot validate the existing receipt, use `candidate_challenge.py submit-final-rework-refresh`, obtain a new independent Midlane decision/export, and run `review_mailbox.py rebind-final-rework-candidate` before preflight. Registration rejects a semantic revision still bound to the prior challenge. Never hand-edit a result or SQLite.

Every state-changing replay launched through `guarded_run.py` must use `--state-changing --preflight-command-file <JSON_ARGV_FILE> --recovery-command-file <JSON_ARGV_FILE> --postcondition-command-file <JSON_ARGV_FILE>`. The same wrapper runs the non-mutating preflight and refuses to launch the child unless the gate exits 0 with a clean owned tree. Recovery must be exact and run-owned. The independent postcondition must check the target-specific listener, process, or equivalent host state without relying on launcher ancestry; `owned_tree_clean` cannot be true until its final check passes. Never compose preflight and mutation with `;` or launch the mutation separately after a failed gate.

Exact public-fix gate: before freezing, registering, or re-registering any package, fetch the relevant stable, maintenance, release-candidate, and main refs. Search the exact vulnerable function, sink, helper, and behavior across those refs, inspect matching commits, pull requests, advisories, and release notes, and machine-verify every branch-currentness claim. An exact public root or remediation published before freeze is a WRITE_OFF and must not be registered, even when the latest shipped stable remains vulnerable.

Use standalone validation during active package construction, when no preflight is being run, with `python tools\review_mailbox\package_safety.py validate --package <PACKAGE_PATH>`. The authoritative registration validation is `package_preflight.py`, which invokes package safety and the required sidecar gates. Do not run a separate `package_safety.py validate` immediately before `package_preflight.py`; that is duplicate work. A modern evidence ZIP has exactly one enclosing root named `folder_of_everything_necessary/`, forward-slash members, and exact byte parity with the loose tree; flat, multi-root, backslash, incomplete, or drifted ZIPs fail. External files must not contain payout economics, risk acceptance, reviewer chatter, mailbox states, local package numbers, `*.pyc`, or `__pycache__`. Run offline Python tests with `PYTHONDONTWRITEBYTECODE=1` from a disposable fresh extraction. Evidence ZIP filenames are at most 86 characters. Rebuild only with `python tools\review_mailbox\package_safety.py rebuild-zip --source <PACKAGE_PATH>\folder_of_everything_necessary --output <ZIP_PATH>`; never delete the prior ZIP first.

Also read and apply `tools/review_mailbox/PRE_FREEZE_PACKAGE_GATE.txt` to the exact bytes. Complete its structural candidate record and map each root-cause/impact claim to raw package evidence, including attacker permissions, victim visibility/denial, negative controls, prior art, and the decisive impact tuple. For a public source repository, search GitHub issues and GitHub pull requests separately for the exact function, exact sink, and exact behavior; record queries, URLs, result, and time. If either sweep is unavailable or incomplete, the candidate cannot PASS. Before preflight, run `python -B tools\review_mailbox\review_mailbox.py candidate-inventory --product <PRODUCT>` and write a private `jenny.candidate-inventory-ack.v1` file covering every returned item and the closest sibling. Refresh the private `jenny.portfolio-admission.v1` record against the same inventory digest and require disposition `PROMOTE`. Store at least one packaged offline or authorized live replay command as private JSON with one `argv` array. Run `python -B tools\review_mailbox\package_preflight.py --package <PACKAGE_PATH> --goal <ACTIVE_GOAL_PATH> --product <PRODUCT> --inventory-ack <PRIVATE_ACK_JSON> --portfolio-admission <PRIVATE_PORTFOLIO_JSON> --candidate-challenge <PRIVATE_CHALLENGE_RESULT_JSON> --result <PRIVATE_RESULT_JSON> --offline-command <PRIVATE_COMMAND_JSON>`; omit `--inventory-ack` only when the inventory is empty, omit `--candidate-challenge` only for a tool-confirmed legacy numbered lineage, and use `--allow-live-replay` only with an authorized `--live-command`. Require schema `jenny.package-preflight.v1`, PASS, exact package/goal/inventory/challenge hashes, a current admitted decision, fresh extraction execution, no external dependency, and unchanged bytes. Register those exact bytes with `python -B tools\review_mailbox\review_mailbox.py register --package <PACKAGE_PATH> --product <PRODUCT> --version <VERSION> --preflight-result <PRIVATE_RESULT_JSON>`. Missing evidence or preflight failure means collect it, narrow the claim, bank it, consolidate/write off a duplicate, or stop; never waive a gate in prose.

For the rest of this goal, treat the mailbox as a checkpoint. Final Reviewer's `queue-final-rework` immediately moves unchanged bytes to ZDI_STAGING. At the start of each goal continuation, after each material package write, and before switching to optional new work, run mailbox status and then `python tools\review_mailbox\review_mailbox.py claim-final-rework` once. The command skips only executable mechanical requests with explicit `MECHANICAL` scope and one shared validated bounded-repair JSON; those remain queued for Midlane. A malformed legacy mechanical request is rerouted to `SEMANTIC` and returned to Hunter rather than stranded. For substantive work, `claim-final-rework` claims the already-staged request, sets FINAL_REWORK, and performs no second package move. If it returns a request, use only that exact request ID and its bound hash/revision. Retrieve the durable payload at any later continuation with `python tools\review_mailbox\review_mailbox.py rework-details --item <ID>`. If an item is QUESTIONS_OPEN, read every question, address the entire batch with hard evidence, refine the actual package yourself, write a private answer JSON under scratch/review_mailbox/, and run the answer command once. Report the exact new frozen hash and stop that refinement tranche for Midlane closure.

When Midlane returns PASS, the mailbox should automatically verify the frozen hash, move the package from ZDI_STAGING into the direct ZDI root, and return AWAITING_FINAL_REVIEW. Do not make a last-minute edit after the pass. If an edit is necessary, the package must be returned to staging, re-registered, and reviewed again. Report every AWAITING_FINAL_REVIEW path to the operator, who will invoke the Final Reviewer manually. Do not modify ZDI/signoff.txt, sealed packages, or submitted packages.

AWAITING_FINAL_REVIEW is not submission. After the operator actually submits and archives a package, only the operator may run `mark-submitted`; Hunter must not infer submission from a missing path or move anything under `ZDI\_SUBMITTED`. `ZDI\signoff.txt` is strict UTF-8 after the operator-authorized 2026-07-13 normalization, with exact prior bytes retained under `notes\review_mailbox\backups`. Never rewrite it ad hoc. If the operator separately authorizes a read or ASCII append, use `python tools\signoff_io.py`; otherwise leave the ledger untouched.

Historical chat JSON is never executable workflow authority. If the operator pastes or refers to Final Reviewer JSON, do not serialize it, call `return-for-rework`, move a package, or infer a fresh request. State that Final Reviewer must log it through `queue-final-rework`, then continue from SQLite. The legacy `return-for-rework` command is disabled. For a claimed request, address every durable issue, update the external-facing package, validate it, and re-register its staging path. Changed registration records the request as ADDRESSED and creates a new READY_FOR_MIDLANE revision; later independent review plus operator `mark-ready` records VERIFIED. Do not replay an ADDRESSED, VERIFIED, CLOSED_HOLD, or CANCELLED request.

If a claimed NEEDS WORK issue explicitly says to return the package as HOLD when a required condition cannot be proved, and your completed audit establishes that it cannot be met, do not modify or re-register unchanged package bytes. Serialize a private JSON object containing `summary` and `evidence_refs`, then run `python tools\review_mailbox\review_mailbox.py hold-final-rework --item <ID> --input <JSON_PATH>`. Confirm terminal HOLD, request state CLOSED_HOLD, and unchanged staging bytes. Do not queue or claim rework for a verdict that was already terminal HOLD or WRITE OFF; report those directly to the operator.

If substantive rework changes the numbered staging folder name, immediately after renaming that exact folder run `python -B tools\review_mailbox\review_mailbox.py rebind-final-rework-path --item <ITEM_ID> --package <NEW_STAGING_PATH>` and verify the returned FINAL_REWORK path before continuing. Never leave SQLite tracking a missing pre-rename path until final registration.
