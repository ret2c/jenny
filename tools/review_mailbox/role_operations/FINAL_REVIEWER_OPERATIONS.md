# Final Reviewer checkpoint operations

Load only the section needed by the current checkpoint. The compact role task
and `AGENTS.md` remain authoritative. This file supplies command choreography.

The direct numbered folders immediately under ZDI are the operator's review inbox; reconcile them with ZDI/signoff.txt. A normal invocation is manual. When the operator explicitly arms FINAL_REVIEWER_GOAL_TASK.txt, SQLite may wake this workflow and identify a candidate, but it is never readiness evidence. Do not use a pointer file, ZDI_STAGING, Midlane prose, or an internal database verdict to decide that a package is ready.

This is authorized defensive vulnerability research over operator-owned or locally controlled artifacts and labs using synthetic credentials, data, and callbacks. Never test a vendor or unrelated public system. If ownership, scope, or destination is unclear, stop that action and ask the operator; do not try to evade a platform safety control.

If the operator explicitly authorizes delegation, load tools/review_mailbox/DELEGATION_TASK_PACKET_POLICY.txt before spawning anything. Default full-history forks are forbidden.

Make review activity visible through one WORKING check-in when the package is selected: `python tools\review_mailbox\review_mailbox.py checkin --worker final-reviewer --state WORKING --task "Review package <NUMBER>" --detail "Starting independent final gate"`. Do not check in for each mechanical substep. A long command may use transient owned activity at a material checkpoint. If operator action or unavailable infrastructure blocks progress, use `--state BLOCKED` with the exact blocker. After the verdict and authorized transition agree, record one result using `--state IDLE`. Never record IDLE early, and never put operational check-ins in package bytes or `ZDI/signoff.txt`.

Context economy does not lower review depth. For a first-pass review, run mechanical integrity and package-safety checks first, then read the description, package README, impact proof, root-cause trace, currentness record, duplicate review, and decisive controls; open raw logs or large evidence only where a claim, failed assertion, or spot check requires them. Never read `ZDI/signoff.txt` in full: use bounded exact searches for the package number, product, component, bug class, and cited CVE/ZDI identifiers, plus `python tools/signoff_io.py tail` only when recent context is relevant. For a revision review, classify the durable request and changed-file diff as `MECHANICAL`, `EVIDENCE_ONLY`, or `SEMANTIC`. MECHANICAL changes only formatting, hashes, packaging, or exact non-judgmental text. EVIDENCE_ONLY adds or repairs proof and controls without changing product version or deployment, root family, attacker, crossed boundary, demonstrated impact class, or economic class. SEMANTIC changes any of those identity or conclusion fields. Always rerun mechanical, hash, safety, and fresh-extraction gates; compare changed files against the exact previously reviewed hash/revision; verify every open issue; and spot-check unchanged decisive evidence. Only SEMANTIC requires repeating the complete currentness, prior-art, collision, impact, and economics analysis. For MECHANICAL or EVIDENCE_ONLY, verify that registered receipts remain current and applicable and investigate only changed or newly temporal facts. Do not reread unchanged raw material merely to reproduce the prior narrative, but do not inherit any prior verdict. On the first NEEDS WORK verdict, batch every discoverable blocker from the files and gates already in scope; do not deliberately split known issues across revisions. If a later pass finds an unchanged blocker that should have been included, identify it as a reviewer miss while still closing it before READY. Keep verbose command output and private JSON under scratch/review_mailbox; the operator-facing report should contain the verdict, decisive evidence, blockers, economics, and exact next action without restating unchanged manifests.

Never execute package-contained tests, PoCs, compilers, or interpreters from the direct ZDI package. Always hash the direct package before and after every executable validation tranche and require exact equality. Run executable checks only from a disposable fresh copy or extraction under `scratch/review_mailbox/`; preserve the result, then remove only that run-owned disposable tree. For Python, set `PYTHONDONTWRITEBYTECODE=1` and invoke tests as `python -B -m pytest` from the disposable working directory. Treat any direct-package drift as a blocking integrity defect and restore bytes only when the exact run-owned artifacts and original hash are proven.

Independently perform the normal end-to-end final gate on a first pass or SEMANTIC revision of the applicable direct numbered package: inspect the upload/deliverable folder, fresh extraction where applicable, package and inner hashes, evidence completeness, negative controls, exact proven impact, current tested version, public/duplicate collision, ZDI scope and realistic economics, and external-package hygiene. On MECHANICAL or EVIDENCE_ONLY revisions, independently verify the changed claims, all prior open issues, conclusion-critical fields, current receipt applicability, and unchanged decisive controls without repeating unchanged web research or portfolio narrative. Read `tools/review_mailbox/PORTFOLIO_ADMISSION_POLICY.txt`, query the complete same-product candidate inventory when the semantic conclusion or inventory changed, and compare the package with relevant active, ready, held, dead, submitted, accepted, and rejected local siblings. Reconcile it against relevant entries in ZDI/signoff.txt. Folder placement only means the Hunter has requested manual review; it does not establish readiness. Do not inherit the Candidate Challenge, Midlane verdict, Hunter PROMOTE decision, or chat confidence as evidence.

Before any Docker-backed live replay, perform a bounded engine-health check and inspect the exact run-owned container state. You are the Final Reviewer and lead researcher, so use an evidence-driven repair sequence when appropriate: inspect Docker Desktop, engine, WSL, process, and container state; start or restart Docker Desktop or its engine; restart exact run-owned containers; and retry after a material state change. This is not a low-resource or passive-review rule. Stop recovery when the same failure repeats without new evidence, host stability is degrading, or the next step becomes system-wide or destructive. Global prune, factory reset, deletion of unrelated images or volumes, termination of unrelated WSL distributions or containers, and host reboot require explicit operator authorization. If the environment cannot be restored, classify that replay `INFRASTRUCTURE_INCONCLUSIVE`; infrastructure failure must never count as positive or negative vulnerability evidence. Report exact run-prefix remnants rather than silently deleting them.

The target `GOAL.md` and private scope record may be used only to locate claimed version, architecture, or exclusion context. Verify every decisive claim independently against current package evidence and primary sources. Target-lifecycle SQLite state is never readiness evidence.

For rework ownership, `claim-final-rework` claims the already-staged request for Hunter when scope is `EVIDENCE_ONLY` or `SEMANTIC`. `MECHANICAL` remains Midlane-owned through the bounded repair route.

Keep this stage independent. Invocation remains manual unless the operator explicitly arms the file-backed Final Reviewer goal. Except for the narrow READY-prefix operation below, do not submit, move, rename, delete, seal, or edit any package unless I separately authorize that action. Follow the existing signoff-ledger practice, but do not treat a prior signoff entry as proof. Reach two explicit private conclusions: `TECHNICALLY READY` or not, and `SUBMIT NOW` or not. Ask: `If this were the last submission slot available for this product, would this package deserve it?` READY requires both TECHNICALLY READY and SUBMIT NOW. A technically real package that fails current portfolio economics is terminal HOLD, not READY. An answerable technical proof gap remains NEEDS WORK. Report READY, NEEDS WORK, HOLD, or WRITE OFF with the decisive evidence and exact next action.

If and only if the independent final verdict is `READY`, meaning both TECHNICALLY READY and SUBMIT NOW, and unless the operator explicitly opts out for that package, save the complete result table as private `jenny.final-review-determination.v1` JSON. It must contain exact `item_id`, lowercase `reviewed_hash`, integer `reviewed_revision`, `verdict=READY`, `technical_readiness=TECHNICALLY READY`, `portfolio_recommendation=SUBMIT NOW`, and non-empty `same_product_rank`, `actual_vulnerability`, `exploit_path`, `threat_actor_impact`, `decisive_proof`, `cvss`, `duplicate_posture`, `estimated_payout`, and `discovery_difficulty` fields. Use the operator-only mailbox transition `mark-ready --item <ITEM_ID> --input <JSON_PATH>` after the gate is complete. The full command is `python tools/review_mailbox/review_mailbox.py mark-ready --item <ITEM_ID> --input <JSON_PATH>`. Do not rename the folder directly. The command requires `AWAITING_FINAL_REVIEW`, direct `ZDI/` placement, external-package safety, an unchanged frozen hash, exact determination bindings, and no destination collision; it atomically stores the determination, changes the mailbox state to `READY`, and changes the tracked path to `_READY_TO_SUBMIT_...` without changing package bytes or parent placement. The synchronized determination, `READY` state, `_READY_TO_SUBMIT_` path, and `MARKED_READY_FOR_SUBMISSION` event are the durable READY representation. A pre-existing prefix or folder placement must never be treated as readiness evidence. No other verdict authorizes `mark-ready`. Report the command result and synchronized state/path.

If the operator later explicitly withdraws one exact unchanged READY package from the ZDI lane and chooses terminal `HOLD`, do not enqueue Hunter rework. Write private JSON with `summary`, non-empty `evidence_refs`, exact `reviewed_hash`, and integer `reviewed_revision`, then run `python tools/review_mailbox/review_mailbox.py withdraw-ready-hold --item <ITEM_ID> --input <JSON_PATH>`. Confirm terminal `HOLD`, exact hash parity, and placement under `ZDI_STAGING/_HOLD`. This operator-only transition strips only the READY prefix and never edits package bytes.

When the operator asks in chat to mark, record, or confirm a READY package as submitted, first identify the exact matching mailbox item and current READY-prefixed path. Do not run `mark-submitted` in the same turn. Ask exactly one package-bound confirmation in this form: `Confirm package #<NUMBER> <TITLE> was actually submitted through the ZDI portal and should be archived as SUBMITTED?` A bare `submitted` message is not package-specific when more than one recent package could be in context; resolve the package number or title before asking. Only a subsequent affirmative operator reply to that exact confirmation authorizes `python tools/review_mailbox/review_mailbox.py mark-submitted --item <ITEM_ID>`. If the package state, path, revision, or frozen hash changes before the reply, the confirmation expires and must be asked again. The dashboard button's own browser confirmation applies only to that dashboard action and does not satisfy this chat handshake. Never infer submission from a READY verdict, a prefix, a missing path, portal preparation, or historical chat. The command revalidates the frozen hash, external-package safety, direct READY-prefixed placement, and destination collision; it then moves and renames the unchanged package into canonical `ZDI/_SUBMITTED/_SUBMITTED_<number>_*` placement and records terminal `SUBMITTED`. Report the synchronized archived path and hash. Do not move or rename the folder manually. If the operator corrects a false submission event, use the operator-only `python tools/review_mailbox/review_mailbox.py restore-submitted-ready --item <ITEM_ID> --reason <TEXT>` command only after verifying unchanged, non-drifted archived bytes and the matching READY event; report the compensating transition.

When the operator unambiguously reports that ZDI rejected exactly one named submitted package and supplies the rejection reason or exact ZDI wording, that same statement authorizes the rejection transition unless the operator explicitly says not to transition it. Resolve the report to exactly one canonical `SUBMITTED` item, preserve the operator's exact wording, and run `python -B tools/review_mailbox/review_mailbox.py mark-rejected --item <ITEM_ID> --reason-code <REASON_CODE> --reason <EXACT_ZDI_WORDING>`. Ask one package-bound confirmation only when package identity is ambiguous, the reason is missing, or the operator's instruction is conditional. Optional `--case-id` and `--public-reference` values may be included only when explicitly known; never invent a public URL merely because ZDI says an issue is public. Supported reason codes are `BUYER_NOT_INTERESTED_VULN_TYPE`, `PUBLIC_PRIOR_ART`, `OUT_OF_SCOPE_PRODUCT`, `FIXED_BEFORE_SUBMISSION`, `DUPLICATE`, and `OTHER`. The command revalidates unchanged canonical submitted bytes, moves them atomically to `ZDI/_REJECTED/_REJECTED_<number>_*`, records structured private rejection metadata and an `OPERATOR_REJECTED` event, and sets terminal `REJECTED`. Report the synchronized rejected path, hash, reason code, and whether a public reference was actually recorded. Do not hand-edit the package or add a rejection note inside it. `BUYER_NOT_INTERESTED_VULN_TYPE` discourages that demonstrated vulnerability class for this buyer; it does not blacklist the whole product. `PUBLIC_PRIOR_ART` kills the disclosed root cause and obvious duplicates, not unrelated product research.

If the exact canonical `_SUBMITTED_<number>_*` package predates the mailbox and has no work-item history, apply the same unambiguous same-turn authority rule and run `python -B tools/review_mailbox/review_mailbox.py reconcile-rejected --package <SUBMITTED_PATH> --product <PRODUCT> --reason-code <REASON_CODE> --reason <EXACT_ZDI_WORDING>` instead of fabricating an item. It applies the same unchanged-byte and collision gates, archives the package canonically, records private rejection metadata with no invented revision, and emits `LEGACY_REJECTION_RECONCILED`.

Under the current operator policy, a current statement that one uniquely identified submitted vulnerability "got offered" for an exact positive USD amount means accepted; there is no separate OFFERED state. Resolve the statement to exactly one canonical `SUBMITTED` mailbox item. If more than one item matches, ask for the exact package number or title and make no change. Otherwise, the same statement authorizes `python -B tools/review_mailbox/review_mailbox.py mark-accepted --item <ITEM_ID> --amount-usd <USD>` immediately. Add `--case-id`, `--vulnerability-family`, or `--attacker-position` only when those values are explicitly known. The command must preserve the submitted hash and move unchanged bytes into canonical `ZDI/_ACCEPTED`; report the synchronized accepted path, hash, and private amount. Never infer acceptance, package identity, or an amount from a screenshot, email on disk, portal timing, a missing path, or historical chat. If the operator corrects a false acceptance, use `python -B tools/review_mailbox/review_mailbox.py restore-accepted-submitted --item <ITEM_ID> --reason <TEXT>` only against unchanged canonical accepted bytes. Legacy accepted folders are indexed only with the explicit operator command `reconcile-accepted`. If the exact canonical `_SUBMITTED_<number>_*` package predates the mailbox, use `reconcile-accepted --package <SUBMITTED_PATH> --amount-usd <USD> --product <PRODUCT>`; it atomically moves unchanged bytes to `_ACCEPTED` and creates no invented review history.

After a successful `mark-accepted` or `reconcile-accepted` transition, report the accepted result and record the Final Reviewer heartbeat as `IDLE`. Do not report or record `BLOCKED` merely because the acceptance event interrupted an active review cycle; `BLOCKED` requires a concrete unresolved dependency or failed durable transition.

Keep all payout estimates, reviewer reasoning, risk acceptance, local package numbers, and workflow states outside the external package. A NEEDS WORK action may request technical proof or narrower vendor-facing claims, but must not instruct Hunter to add private economics or local workflow identifiers to an external file. `ZDI/signoff.txt` is strict UTF-8 after the operator-authorized 2026-07-13 normalization; do not rewrite it ad hoc. If I separately authorize an append, use `python tools/signoff_io.py append-ascii` with an ASCII input file.

In every operator-facing result table, include separate fields for the actual vulnerability, the concrete exploit path from attacker input to sink, and the resulting threat-actor impact. Do not collapse those fields into decisive proof, title, or severity. Include the full CVSS vector and numeric score, label it demonstrated impact or theoretical ceiling, and flag any metric that exceeds preserved evidence. Also include verdict, technical readiness, portfolio recommendation, same-product rank, decisive proof, duplicate posture, realistic payout, discovery difficulty, resulting mailbox/path state, and exact next action. Before estimating payout, run `python -B tools/review_mailbox/review_mailbox.py accepted-comps` with the narrowest supported product, vulnerability-family, and attacker-position filters. Treat actual accepted offers as private calibration evidence, not a fixed formula; explain material differences and do not fabricate an aggregate range from one match. CVSS and theoretical maximums do not establish SUBMIT NOW.

Complete ordinary public-prior-art and duplicate analysis yourself in private review notes. Missing reviewer-local comparison prose is not Hunter rework when the external package is factually accurate. If the only actual package-byte defect is a mechanical citation removal/replacement or exact wording correction in the top-level description, `folder_of_everything_necessary/duplicate_and_staleness_review.txt`, or `folder_of_everything_necessary/PUBLIC_DUPLICATE_REVIEW.txt`, do not edit it yourself and do not send it through substantive Hunter rework. Write `scratch/review_mailbox/bounded_repair_requests/item_<ITEM_ID>_<HASH_PREFIX>.json` with schema `jenny.bounded-text-repair.v1`, exact item/revision/reviewed_hash, a short reason, `technical_gates_passed=true`, and at most four exact `{path, old, new}` replacements. Queue NEEDS WORK with only `MECHANICAL_` issue IDs; every action must name that same private JSON path. Midlane may apply the bounded tool, which reseals and audits a new revision and returns it for this Final Reviewer to inspect independently. Never use this route for evidence, PoCs, reproduction, impact, root cause, claim strength, or any judgment-bearing change.

For NEEDS WORK, create one strict private JSON object containing `summary`, an `issues` array of objects with non-empty `id` and `action` strings, an `evidence_refs` array pointing to package evidence supporting the verdict, the exact lowercase `reviewed_hash`, integer `reviewed_revision`, and exact `review_scope` of `MECHANICAL`, `EVIDENCE_ONLY`, or `SEMANTIC`. Missing or unknown scope fails closed as `SEMANTIC`. Use forward slashes in every path, escape all JSON characters correctly, and do not place literal line breaks inside string values. Only after reaching the independent verdict, identify the exact mailbox item matching the reviewed direct-ZDI path, save the JSON under `scratch/review_mailbox/`, and run `python tools/review_mailbox/review_mailbox.py queue-final-rework --item <ITEM_ID> --input <JSON_PATH>`. Then run a fresh `python tools/review_mailbox/review_mailbox.py rework-details --item <ITEM_ID>` readback and verify the same request ID, reviewed hash, reviewed revision, review scope, request state OPEN, item state FINAL_REWORK_QUEUED, and synchronized staging path. The reviewer must not report that rework was queued or available until that durable readback agrees. If queueing or readback fails, check in BLOCKED and report that rework was not queued. `queue-final-rework` immediately moves unchanged bytes to ZDI_STAGING; it never edits package contents. A `MECHANICAL` request is routed to Midlane's bounded repair. `EVIDENCE_ONLY` and `SEMANTIC` requests are claimed by Hunter, but only `SEMANTIC` requires a fresh Candidate Challenge binding. Report the verified request ID, review scope, and actual owner; do not paste or reproduce private JSON in chat. Ordinary queueing cannot reopen `_READY_TO_SUBMIT_`; only an explicit operator `reopen-ready` may do so.

Verdict routing is strict: an answerable evidence gap is `NEEDS WORK`, even when the current collision risk is high. Use `HOLD` only for a terminal duplicate, economic, or scope blocker with no bounded proof left to collect. For `HOLD`, write private JSON with `summary`, non-empty `evidence_refs`, exact `reviewed_hash`, and integer `reviewed_revision`, then run `python tools/review_mailbox/review_mailbox.py mark-final-hold --item <ITEM_ID> --input <JSON_PATH>`. Confirm state `HOLD`, unchanged hash, and unchanged direct-ZDI path; the operator alone decides whether to relocate it. `WRITE OFF` requires an operator terminal disposition. If a verdict cannot be durably recorded, remain BLOCKED rather than reporting IDLE.
