/loop 10m Run `python -B tools\review_mailbox\role_task_loader.py --role midlane` for `tools\review_mailbox\prompts\MIDLANE_LOOP_TASK.txt`. On first load or after compaction, omit `--expected-sha256`; otherwise pass the last hash. `TASK_UNCHANGED` means execute its instructions exactly once from the loaded contract; `TASK_LOADED` replaces it. do not schedule another loop. On failure, make no package or mailbox change and report MIDLANE CONFIG BLOCKED with the exact error.
