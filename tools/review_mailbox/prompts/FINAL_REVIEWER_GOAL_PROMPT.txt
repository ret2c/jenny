/goal Run `python -B tools\review_mailbox\role_task_loader.py --role final-goal` for `tools\review_mailbox\prompts\FINAL_REVIEWER_GOAL_TASK.txt`. On first load or after compaction, omit `--expected-sha256`; otherwise pass the last hash. `TASK_UNCHANGED` means continue the loaded task; `TASK_LOADED` replaces it. Execute continuously until paused or cleared. Do not create a scheduled task, /loop, second goal, daemon, API client, or new chat.
