Run `python -B tools\review_mailbox\role_task_loader.py --role final`. On first load or after compaction, omit `--expected-sha256`; otherwise pass the last loaded hash. `TASK_UNCHANGED` means execute the already-loaded FINAL_REVIEWER_TASK.txt; `TASK_LOADED` replaces it. Execute one independent final-review cycle against direct ZDI. On load failure, make no package or mailbox change and report the exact configuration error.
