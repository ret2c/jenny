Run `python -B tools\review_mailbox\role_task_loader.py --role hunter`. On first load or after compaction, omit `--expected-sha256`; otherwise pass the last loaded hash. `TASK_UNCHANGED` means execute the already-loaded HUNTER_TASK.txt without rereading it. `TASK_LOADED` replaces that contract. Apply it to the exact target GOAL.md selected by my current instruction. If loading fails, make no target, package, or mailbox change and report the exact configuration error.
